# Dangerous Meeting

Note: In progress. Website not finished.

Website for Dangerous Meeting, migrated to Next.js, React and TypeScript.{}

## Scripts

- `npm run dev` starts the local development server on `http://localhost:3001`.
- `npm run build` creates the production build.
- `npm run start` runs the production server on `http://localhost:3001` after a build.
- `npm run lint` checks the project with ESLint.

## Routes

- `/`
- `/servicios`
- `/nosotros`
- `/contacto`
